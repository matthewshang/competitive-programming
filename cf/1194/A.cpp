#include "bits/stdc++.h"

using namespace std;

using ll = long long;

int main() {
    ios::sync_with_stdio(0); cin.tie(0);

    int T; cin >> T;
    for (int tt = 0; tt < T; tt++) {
        int n, x; cin >> n >> x;
        cout << x * 2 << endl;
    }

    return 0;
}
